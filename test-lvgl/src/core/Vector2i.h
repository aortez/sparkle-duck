#pragma once

// Vector2i is now an alias for Vector2<int>.
// All implementation is in Vector2.h (fully inline template).
#include "Vector2.h"

// Backward compatibility: Vector2i is defined in Vector2.h as:
// using Vector2i = Vector2<int>;
